# PolicyAssist RAG Backend – copy to .env and fill in

# Required: LLM and embeddings (OpenAI or any OpenAI-compatible API)
OPENAI_API_KEY=sk-your-key-here

# Optional: use a different base URL (e.g. Azure, local model)
# OPENAI_BASE_URL=https://your-api.com/v1

# Optional: model names
# LLM_MODEL=gpt-4o-mini
# EMBEDDING_MODEL=text-embedding-3-small

# Optional: chunk size for document splitting
# CHUNK_SIZE=1000
# CHUNK_OVERLAP=200
